# TeleHubX 工程技术蓝图

> **本文档目标**：让任何工程师或运维新人在 30 分钟内完整理解 TeleHubX 的技术架构、关键流程、数据流、端口、文件位置、安全模型，并能精准定位每一个组件的代码位置。
>
> **最后更新**：2026-05-09（vmfix25）
> **状态**：单租户部署可 ship；Phase 6 SaaS 多租户 api_id 池待实施

---

## 目录

1. [项目身份与定位](#1-项目身份与定位)
2. [整体架构](#2-整体架构)
3. [技术栈](#3-技术栈)
4. [端口与进程清单](#4-端口与进程清单)
5. [文件系统布局](#5-文件系统布局)
6. [服务生命周期](#6-服务生命周期)
7. [License 系统](#7-license-系统)
8. [认证与登录](#8-认证与登录)
9. [多租户架构](#9-多租户架构)
10. [Telegram 集成](#10-telegram-集成)
11. [数据库 Schema](#11-数据库-schema)
12. [核心业务模块](#12-核心业务模块)
13. [AI 集成](#13-ai-集成)
14. [安装包架构](#14-安装包架构)
15. [.env 加载链与候选优先级](#15-env-加载链与候选优先级关键易错点)
16. [SeedPack 内置素材](#16-seedpack-内置素材)
17. [安全模型](#17-安全模型)
18. [构建与发布流程](#18-构建与发布流程)
19. [已知问题与未来工作](#19-已知问题与未来工作)
20. [快速排查手册](#20-快速排查手册)

---

## 1. 项目身份与定位

| 维度 | 值 |
|------|---|
| 项目名 | **TeleHubX** |
| 仓库 | `C:\AI_WORKSPACE\Telegram Auto Bot` |
| GitHub | https://github.com/bryangeh79/TeleHubX |
| Git 主分支 | `main` |
| 平台 | Windows 10/11（x64） |
| 部署模式 | 单机本地化部署（每个租户一台 Windows 物理机/VM） |
| **定位** | Telegram 广告投放 + 多账号养号 + AI 智能客服 + 人工接管 + 线索收集 的 SaaS 系统 |
| **不是** | 单纯的 Telegram 群发器 |
| 核心商业逻辑 | 广告号引流 → 客服号成交 |
| 参考项目 | WAhubX（`C:\AI_WORKSPACE\Whatsapp Auto Bot`，**只读不写**） |

### 三种 TG 账号角色（核心创新）

| 角色 | 行为 | 风险 |
|------|------|------|
| **客服号 (cs)** | AI 自动回复 + FAQ + 接管，**被动**等待客户 | 低 |
| **广告号 (ad)** | 关闭 AI；群组 FAQ 简短回复；私聊统一引流到 Bot；执行 Campaign / 剧本 / Warmup | 中 |
| **混合号 (hybrid)** | 高风险，需 Super Admin 显式覆盖才启用 | 高 |

---

## 2. 整体架构

### 高层组件图

```
┌──────────────────────────── Windows Service (TeleHubX) ────────────────────────────┐
│                                                                                       │
│  WinSW (telehubx-service.exe)                                                        │
│      │  Auto-start                                                                    │
│      ▼  Identity: NT AUTHORITY\LocalService                                          │
│  telehubx-supervisor.exe (Node SEA)                                                  │
│      │                                                                                │
│      ├─► postgres.exe        (port 5436, via pg_ctl, restricted token)               │
│      │       └─ data: %ProgramData%\TeleHubX\data\pgdata                            │
│      ├─► redis-server.exe    (port 6386)                                             │
│      ├─► node main.js        (apps/server, NestJS, port 9800)                        │
│      ├─► node main.js        (apps/agent, GramJS multi-account)                      │
│      └─► node serve.cjs      (apps/dashboard, port 9601, reverse proxy /api → 9800)  │
└───────────────────────────────────────────────────────────────────────────────────────┘
                                        ▲
                                        │ HTTP (loopback only)
                                        │
                              Browser at 127.0.0.1:9601
                                        │
                                        ▼
                              ┌─────────────────┐
                              │  React + Vite   │
                              │  + Ant Design 5 │
                              └─────────────────┘
                                        │
                                        ▼ HTTPS
                  ┌───────────────────────────────────────┐
                  │  License Worker (Cloudflare)          │
                  │  https://telehubx-license             │
                  │       .starbright-solutions.com       │
                  │  - /licenses/issue (admin)            │
                  │  - /licenses/activate                 │
                  │  - /licenses/verify (periodic)        │
                  │  - /agents/heartbeat (periodic)       │
                  └───────────────────────────────────────┘
                                        │
                                        ▼
                  ┌───────────────────────────────────────┐
                  │  Telegram MTProto API                 │
                  │  - GramJS (TypeScript)                │
                  │  - StringSession                      │
                  │  - SOCKS5 proxy per account           │
                  └───────────────────────────────────────┘
```

### 进程层级（运行时）

```
SCM (Service Control Manager)
└── WinSW (telehubx-service.exe)            [System Mandatory Level + LocalService]
    └── telehubx-supervisor.exe              [Node SEA, manages children]
        ├── postgres.exe                     [via pg_ctl, restricted token to drop admin]
        │   └── 5x backend processes (writer/checkpointer/bgwriter/walwriter/logger)
        ├── redis-server.exe
        ├── node.exe (server)                [NestJS, listens 9800]
        ├── node.exe (agent)                 [GramJS poll loop, only if TG_API_ID set]
        └── node.exe (dashboard serve.cjs)   [Static + reverse proxy, listens 9601]
```

---

## 3. 技术栈

### 后端 (`apps/server/`)

| 层 | 选型 | 备注 |
|----|------|-----|
| Runtime | Node.js 20 (portable, shipped in installer) | `vendor/node-v20-win-x64\node.exe` |
| 框架 | **NestJS 10** | TypeScript |
| ORM | TypeORM | `synchronize: true` 在生产（待迁 migrations） |
| DB | **PostgreSQL 16.4 + pgvector** | 单 schema 多租户，未来 schema-per-tenant |
| Cache/Queue | **Redis 5.0.14.1**（tporadowski/redis Windows 版） | BullMQ 队列 |
| Auth | scrypt 密码哈希 + 自实现 HS256 JWT | 不依赖 `@nestjs/jwt` |
| WebSocket | `@nestjs/platform-socket.io` | 实时人工接管通道 |

### TG 客户端

| 选型 | 用法 |
|------|------|
| **GramJS (TypeScript)** | MTProto 客户端，多账号池 |
| StringSession | session 序列化（AES-GCM 加密存盘） |
| SOCKS5 代理 | 每个号绑死一个固定住宅/移动 IP |
| Bot API（仅客服入口） | 平台单一 Bot，长轮询 25s |

### Agent (`apps/agent/`)

| 选型 | 用法 |
|------|------|
| Node 20 + dotenv | DB 驱动，30s 轮询 `/accounts` 加载所有 `sessionEncrypted=true` 的账号 |
| GramJS 多 client 池 | Slot 编号稳定，release→reset 门控 |
| KeepOnline | 心跳保活 |
| BehaviorSimulator | Gaussian 间隔模拟人类操作 |

### 前端 (`apps/dashboard/`)

| 选型 | 备注 |
|------|------|
| **React 18** + Vite 6 | TS + JSX |
| **Ant Design 5** | UI 组件 |
| React Router 6 | SPA 路由 |
| Axios | HTTP 客户端 |
| **Socket.IO Client** | 人工接管实时消息 |
| react-i18next | 4 语言（zh/en/ms/vi）已挂载，部分翻译 |
| Zustand（计划） | 状态管理 |

### 安装/分发

| 选型 | 用法 |
|------|------|
| **Inno Setup 6** | Windows installer，含 [Code] Pascal 脚本（向导页 + 服务身份强制） |
| **WinSW v2.12** | Windows Service 包装器（`installer/runtime/winsw/`） |
| **Node SEA** | Single Executable App，把 supervisor + stop 打包成 .exe |
| `mshta.exe` + HTA | 启动加载窗（vmfix18） |

### License

| 选型 | 用法 |
|------|------|
| **Cloudflare Worker** | 无服务器 license server |
| **Ed25519 签名** | License Worker 给每个 license 签名 |
| AES-256-GCM | agent_token / session 本地加密 |

---

## 4. 端口与进程清单

| 端口 | 协议 | 进程 | 用途 | 暴露范围 |
|------|------|------|------|---------|
| **9601** | HTTP | `node serve.cjs` | Dashboard 静态服务 + 反向代理 `/api/*`、`/ws/*`、`/socket.io/*` → 9800 | **127.0.0.1 only** |
| **9800** | HTTP | `node main.js` (server) | NestJS API 主入口 + WebSocket | **127.0.0.1 only** |
| **5436** | TCP | `postgres.exe` | PostgreSQL 监听 | **127.0.0.1 only** |
| **6386** | TCP | `redis-server.exe` | Redis 监听 | **127.0.0.1 only** |

**所有端口都仅 loopback 监听**，无对外暴露。租户在浏览器 `http://127.0.0.1:9601/` 访问。

### 服务/进程关键路径

| 进程 | 路径 |
|------|------|
| `telehubx-service.exe` (WinSW) | `C:\Program Files\TeleHubX\tools\telehubx-service.exe` |
| `telehubx-supervisor.exe` (Node SEA) | `C:\Program Files\TeleHubX\tools\telehubx-supervisor.exe` |
| `telehubx-stop.exe` (Node SEA) | `C:\Program Files\TeleHubX\tools\telehubx-stop.exe` |
| `postgres.exe` | `C:\Program Files\TeleHubX\runtime\postgres\bin\postgres.exe` |
| `pg_ctl.exe` | `C:\Program Files\TeleHubX\runtime\postgres\bin\pg_ctl.exe` |
| `redis-server.exe` | `C:\Program Files\TeleHubX\runtime\redis\redis-server.exe` |
| `node.exe` (children) | `C:\Program Files\TeleHubX\runtime\node\node.exe` |

---

## 5. 文件系统布局

### 安装目录（程序文件，只读）

```
C:\Program Files\TeleHubX\
├── apps\
│   ├── server\          # NestJS 编译产物 + flat node_modules
│   ├── agent\
│   └── dashboard\       # Vite 编译产物 + serve.cjs
├── tools\
│   ├── telehubx-service.exe       # WinSW 重命名版
│   ├── telehubx-service.xml       # WinSW 服务描述符
│   ├── telehubx-supervisor.exe    # Node SEA 主管
│   ├── telehubx-stop.exe          # Node SEA 停止工具
│   ├── telehubx-start.vbs         # 桌面启动快捷方式调用（sc start + mshta）
│   ├── telehubx-stop.vbs          # 桌面停止
│   ├── telehubx-debug.vbs         # 显示日志的 PS 窗口
│   ├── telehubx-loading.hta       # 启动加载窗
│   └── telehubx-open.vbs          # 仅打开浏览器（vmfix20）
├── runtime\
│   ├── node\node-v20-win-x64\node.exe
│   ├── postgres\          # PostgreSQL Portable + pgvector
│   ├── redis\             # Redis-for-Windows
│   └── postgres\init-pgdata.cjs   # postgres 首次初始化脚本
├── assets\
│   ├── telehubx.ico
│   ├── telehubx-banner.bmp
│   └── telehubx-banner-small.bmp
├── .env.template          # 模板，被 bootstrapUserEnv 拷贝到 LocalService userEnv
└── VERSION.txt            # version=vmfixN, commit, buildTime, artifact, serviceIdentity
```

### 数据目录（用户可写）

```
C:\ProgramData\TeleHubX\
├── .env                                # 主 env（vmfix25 wizard 写入此处 + .env.template）
└── data\
    ├── pgdata\                         # postgres 数据目录
    ├── redis-data\                     # redis dump
    ├── sessions\                       # 加密的 GramJS session 备份
    ├── uploads\                        # 知识库 + 广告素材上传
    ├── assets\
    │   └── _builtin\                   # SeedPack 释放此处
    │       ├── images\
    │       │   ├── business_promo\     # 35 张
    │       │   ├── festivals\          # 9 张
    │       │   ├── food_general\       # 19 张
    │       │   ├── reaction_emoji\     # 19 张
    │       │   └── scenery\            # 15 张
    │       ├── voices\                 # 200 段（9 个分类）
    │       └── videos\                 # 25 段精选
    ├── script-packs\                   # SeedPack 释放此处
    │   ├── scripts_pack_ab_zh_v1.json   # 30 个 2 人剧本
    │   └── scripts_pack_4p_zh_v1.json   # 50 个 4 人剧本
    ├── secrets\
    │   └── local-secrets.env            # 自动生成：JWT_SECRET / SESSION_ENCRYPTION_KEY / AGENT_TOKEN
    ├── logs\
    │   ├── supervisor.log               # supervisor 主日志
    │   ├── postgres.log                 # postgres 进程 stdout/stderr
    │   ├── redis.log
    │   ├── server.log                   # Nest 同步日志（main.ts 用 fs.appendFile）
    │   ├── agent.log
    │   ├── dashboard.log
    │   ├── app-YYYY-MM-DD.log           # winston rotation（Nest）
    │   └── error-YYYY-MM-DD.log
    ├── run\                             # 运行时状态
    │   ├── supervisor.lock              # 防双开锁
    │   ├── postgres.pid                 # 健康后才提交
    │   ├── redis.pid
    │   ├── server.pid
    │   ├── agent.pid
    │   └── dashboard.pid
    ├── cloud-license.bin                # AES-GCM 加密的 license state（含 agentToken）
    ├── machine-fingerprint.txt          # 首次绑机的机器指纹
    └── agent-token.bin                  # 单独的 agent token 备份
```

### LocalService 配置文件（**关键**）

```
C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\TeleHubX\
└── .env                  # bootstrapUserEnv() 第一次启动时从模板拷贝过来
                          # supervisor loadSupervisorEnv 候选 #2，优先级高于 ProgramData\.env
                          # **vmfix24 修复了写入路径问题**
```

---

## 6. 服务生命周期

### 启动序列（每次 service 启动）

文件：`installer/tools/src/supervisor.ts`

```
1. WinSW 启动 supervisor.exe（identity = NT AUTHORITY\LocalService）
   ↓
2. supervisor.main()
   ↓
3. loadSupervisorEnv()                      [shared/env.ts]
   ├─ bootstrapUserEnv: 第一次拷 .env.template → %APPDATA%\TeleHubX\.env
   ├─ bootstrapLocalSecrets: 第一次生成 JWT_SECRET 等
   └─ 按候选顺序读 .env 文件设置 process.env（先到先得）
   ↓
4. 写 supervisor.log 第一行: "==== TeleHubX supervisor starting ===="
   ↓
5. 记录 [token] effective groups（vmfix19 诊断）
   ↓
6. 获取 bootstrapLock (data/run/supervisor.lock)
   ↓
7. cleanStalePidFiles: 清掉死进程的 pid 文件
   ↓
8. orphan-postgres cleanup（vmfix21）: 杀残留 postgres.exe + 清 stale postmaster.pid
   ↓
9. 顺序启动 5 个服务，每个走 spawn → wait health → commit pid:
   ┌─────────────┬───────────────────────────────────────────────┐
   │ postgres    │ pg_ctl start -w (vmfix19, 自动 drop admin token)│
   │             │ 读 postmaster.pid 拿真实 pid（vmfix21 重试）    │
   │             │ tcpProbe 5436                                  │
   ├─────────────┼───────────────────────────────────────────────┤
   │ redis       │ spawn redis-server + redis.conf, tcpProbe 6386 │
   ├─────────────┼───────────────────────────────────────────────┤
   │ server      │ spawn node main.js, httpProbe /health          │
   ├─────────────┼───────────────────────────────────────────────┤
   │ agent       │ spawn node main.js（如果 TG_API_ID 空，1 秒后退出）│
   ├─────────────┼───────────────────────────────────────────────┤
   │ dashboard   │ spawn node serve.cjs, httpProbe :9601          │
   └─────────────┴───────────────────────────────────────────────┘
   ↓
10. license-aware browser open（仅交互 session 可见）:
    - configured + active → http://127.0.0.1:9601/
    - unconfigured/locked → http://127.0.0.1:9601/settings/license
   ↓
11. STARTUP COMPLETE → 进入心跳循环（5 分钟一次 log）
   ↓
12. 等待 SIGTERM/SIGINT/SIGBREAK
```

### 停止序列

```
1. SCM 通过 WinSW 发送停止信号
2. supervisor 收到 SIGINT/SIGBREAK → gracefulShutdown()
3. 反序杀子进程：
   dashboard → agent → server → redis → postgres
   （postgres 走 pg_ctl stop -m fast，让它干净清 postmaster.pid）
4. 释放 supervisor.lock
5. 等所有子进程 exit（最多 25 秒）
6. supervisor.exe exit 0
```

---

## 7. License 系统

### 架构

```
┌─────────────────────────┐         ┌────────────────────────────┐
│  License Worker         │         │  TeleHubX Server           │
│  (Cloudflare Worker)    │ HTTPS   │  (CloudLicenseService)     │
│  - tenant 表            │ ◄─────► │  - 本地加密 license state    │
│  - license 表（Ed25519）│         │  - agent_token 持有         │
│  - tenant_user 表       │         │  - 周期 verify + heartbeat  │
└─────────────────────────┘         └────────────────────────────┘
```

### License Key 格式

```
THX-XXXX-XXXX-XXXX
```

由 License Worker 生成，绑定 tenant + Ed25519 签名。**不在客户端存明文**——本地只存掩码 `THX-****-****-XXXX`。

### 激活流程

文件：`apps/server/src/cloud-license/cloud-license.service.ts`

```
1. 用户在 /settings/license 填：License Key + Email + Password
2. Server.activate() 调用 License Worker /licenses/activate
   {
     licenseKey: "THX-...",
     email: "user@example.com",
     password: "...",
     machineFingerprint: <SHA-256 of stable hardware ID>,
     hostname: <os.hostname()>,
     agentVersion: "1.0.0"
   }
3. License Worker 校验：
   - License key 存在 + 未 revoked + 未 expired
   - email + password 匹配 tenant_user
   - 如果 firstBind=true：把 machineFingerprint 绑到 license
   - 否则：machineFingerprint 必须匹配（machine_mismatch 否则拒绝）
4. License Worker 返回：
   {
     licenseId, tenantName, plan, maxAccounts, expiresAt,
     userEmail, userRole,
     agentToken: <一次性返回，AES-GCM 本地加密保存>,
     agentTokenExpiresAt,
     firstBind: bool
   }
5. CloudLicenseService 写 cloud-license.bin（AES-256-GCM, key 来自 SESSION_ENCRYPTION_KEY）
6. **vmfix17**: provisionLocalUser() — 创建/更新本地 users 表
   ├─ 用 AuthService.hashPassword(password)
   ├─ 用 email 作为 username
   ├─ 映射 userRole → UserRole 枚举
   └─ tenantId 关联到 default tenant
7. 返回 status() 给前端
```

### 周期校验

| 操作 | 频率 | 端点 |
|------|------|------|
| **verify** | 每 30 分钟 | `POST /licenses/verify` |
| **heartbeat** | 每 5 分钟 | `POST /agents/heartbeat` |

### 状态机

| effectiveStatus | 触发 | 行为 |
|-----------------|------|------|
| `active` | verify 成功 | 全部功能开放 |
| `grace` | 网络抽风，verify 失败 < 8 次 + < 24 小时 | 仍可用，dashboard 显示警告 |
| `locked` | verify 硬错误（revoked/suspended/expired/machine_mismatch）或 grace 超时 | 阻止新建账号、新任务，老数据可读 |
| `unconfigured` | 还没激活 | 必须先激活 |
| `unknown` | 启动后未读到 state | 跟 unconfigured 等价（vmfix15 修过白屏） |

### 网关代码

| 闸门 | 位置 | 作用 |
|------|------|------|
| `canAddAccount()` | `cloud-license.service.ts:233` | 阻止超 maxAccounts 配额 |
| `canRunTasks()` | 同上 | locked 状态下停任务 |

---

## 8. 认证与登录

### 用户存储

文件：`apps/server/src/auth/user.entity.ts`

```typescript
@Entity('users')
class User {
  id: uuid PK
  username: string UNIQUE       // 通常 = email（vmfix17 mirror）
  passwordHash: string          // scrypt N=16384, r=8, p=1, len=64
  passwordSalt: string          // 16 bytes hex
  role: UserRole                // SUPER_ADMIN | ADMIN | OPERATOR | VIEWER
  tenantId: uuid FK             // nullable
  enabled: boolean
  lastLoginAt: timestamptz
}
```

### Login 流程

文件：`apps/server/src/auth/auth.service.ts`

```
1. POST /api/v1/auth/login {username, password}
2. AuthService.login():
   - users.findOne({username})
   - if (!user || !user.enabled) → 401 Invalid credentials
   - verifyPassword(password, salt, hash) via scryptSync + timingSafeEqual
   - users.update lastLoginAt
   - signToken({sub, username, role, tenantId})
3. 返回 {token, user: {id, username, role, tenantId}}
```

### JWT 实现（自实现 HS256）

```
Header:  {"alg":"HS256","typ":"JWT"}
Payload: {sub, username, role, tenantId, iat, exp}
Sig:     HMAC-SHA256(JWT_SECRET, headerB64 + "." + payloadB64)

JWT_SECRET 来自：
  - 生产: data/secrets/local-secrets.env (bootstrapLocalSecrets 自动生成 64 hex)
  - 开发: .env 里的 JWT_SECRET
TTL: 7 天（JWT_EXPIRES_SEC=604800）
```

### 路由守卫

文件：`apps/server/src/auth/`

| Guard | APP_GUARD | 行为 |
|-------|-----------|------|
| `JwtAuthGuard` | 第一道 | 所有非 `@Public()` 路由必须有 token |
| `RolesGuard` | 第二道 | `@Roles(Role.X)` 装饰器检查角色 |

### 装饰器

| 装饰器 | 含义 |
|--------|------|
| `@Public()` | 不需要 JWT（用于 /auth/login, /cloud-license/status 等） |
| `@Roles(Role.SUPER_ADMIN)` | 限制角色 |
| `@CurrentUser()` | 注入当前用户对象 |
| `@AllowAgent()` | 允许 X-Agent-Token header 通过（agent ↔ server 双向） |

### Roles 优先级

| 角色 | 用途 |
|------|------|
| `SUPER_ADMIN` | 平台运营者（License 签发、跨租户管理） |
| `ADMIN` | 租户管理员（License 激活时默认分配此角色） |
| `OPERATOR` | 普通运营 |
| `VIEWER` | 只读 |

---

## 9. 多租户架构

### 当前状态：**单 schema 多租户**

每个表带 `tenantId` 列（nullable for builtin）。中间件按当前用户 tenantId 过滤。

```sql
-- 示例：accounts 表
CREATE TABLE accounts (
  id UUID PK,
  tenantId UUID NOT NULL,       -- 强制 tenant scope
  phoneNumber TEXT,
  ...
);
```

### Tenant 解析器

文件：`apps/server/src/auth/tenant-resolver.ts`

| 函数 | 用途 |
|------|------|
| `resolveTenantId(user, fallback)` | 写操作（强制必须有 tenantId，无则 throw） |
| `resolveTenantIdSoft(user, fallback)` | 读操作（SUPER_ADMIN 可看全量，普通用户限自己） |
| `callerTenantId(user)` | findOne/update/delete 类（"权属上限"）|

### 规则

| 角色 | 行为 |
|------|------|
| SUPER_ADMIN | 可显式传 query/body.tenantId 跨租户查；不传则用自己的 |
| AGENT (X-Agent-Token) | 必须显式传 tenantId（agent 一个进程服务多租户） |
| 普通用户 | fallback 一律忽略，强制用 user.tenantId |

### Phase 6 规划：**schema-per-tenant**

未来：每个租户独立 PostgreSQL schema，完全行级隔离。

---

## 10. Telegram 集成

### TG API 凭据（关键配置）

环境变量：`TG_API_ID` (整数) + `TG_API_HASH` (32 位 hex)

来源：https://my.telegram.org/apps（用户自己申请一个 App）

**vmfix25 方案**：装包时 wizard 收集，写到：
1. `%ProgramData%\TeleHubX\.env`（supervisor candidate #3）
2. `%ProgramFiles%\TeleHubX\.env.template`（被 bootstrapUserEnv 拷贝到 LocalService userEnv）

读取链：见 [.env 加载链](#15-env-加载链与候选优先级关键易错点)。

### 设备指纹（反检测铁律）

每个 TG 账号绑死一个：
- `deviceModel`: `Samsung SM-S928B`
- `systemVersion`: `Android 14`
- `appVersion`: `Telegram 10.14.2`
- `langCode`: `en`
- `systemLangCode`: `en`

文件：`apps/server/src/accounts/account.entity.ts` `deviceFingerprint` 字段。

### 代理（每号绑死一个 IP）

| 类型 | 支持 |
|------|------|
| **SOCKS5** | 直接用 |
| HTTP/HTTPS | 走本地 socks5-relay 桥透明转 SOCKS5 |
| Datacenter Proxy | **禁用**（高风险） |

### Bind（绑号）流程

文件：`apps/server/src/accounts/bind/bind.service.ts`

```
Step 0: POST /accounts/bind/init {accountId, phone}
  ├─ 校验 TG_API_ID/HASH 已配置（否则 503 + code: 'tg_api_not_configured'）
  ├─ 创建 TelegramClient with phone + apiId + apiHash + StringSession()
  ├─ client.connect()
  ├─ client.sendCode({apiId, apiHash}, phone, codeSettings)
  ├─ 保存 phoneCodeHash 到 active 缓存
  └─ 返回 {phoneCodeHash, expiresIn: 60}

Step 1: POST /accounts/bind/verify {accountId, code, password?}
  ├─ client.signIn({phoneNumber, phoneCode, phoneCodeHash})
  ├─ 如果 SESSION_PASSWORD_NEEDED → 返回 {needsPassword: true, hint}
  ├─ 否则 client.signInWithPassword(...)
  ├─ 拿 client.session.save() 序列化 → AES-GCM 加密保存
  └─ 返回 {ok: true, user: {firstName, username, ...}}

Step 2 (持续): agent 端
  └─ 30s 轮询 /accounts，发现新 sessionEncrypted=true 的账号
  └─ 解密 session string
  └─ 创建 TelegramClient + 挂 message handler + 启动 KeepOnline
  └─ 心跳 lastHeartbeatAt
```

### Bot Gateway（客服入口）

文件：`apps/server/src/bot-gateway/`

```
- 单一平台 Bot（用户配置 botToken via /tenants/bot）
- 25s 长轮询 (Telegram Bot API getUpdates)
- 收到客户消息 → AutoReplyDecider:
  - off / faq / smart 三档
  - tenant 自有 AI key 优先，平台 fallback
- Takeover (人工接管) 即时停 AI + 阻断广告任务
```

### Group Lead Collector（agent 端）

被动监听群组消息，按结构化字段（tg_username、关键字、产品、规模、预算、意向）提取潜在客户写入 leads 表。

---

## 11. 数据库 Schema

主要表（不完整，按重要性排序）：

| 表 | 用途 |
|----|------|
| `users` | 平台用户（admin/operator） |
| `tenants` | 租户 |
| `tenant_settings` | 租户级配置（AI key、greeting 等） |
| `tenant_bots` | Bot Gateway token |
| `licenses` | License Worker 镜像（暂用） |
| `accounts` | TG 账号（cs/ad/hybrid） |
| `proxies` | SOCKS5 代理池 |
| `slots` | 账号 → agent 进程 slot 的稳定编号 |
| `campaigns` | 广告 4 层架构第 1 层（计划） |
| `ad_templates` | 第 2 层（素材模板） |
| `tasks` | 第 3 层（执行单） |
| `task_attributions` | 第 4 层（归因） |
| `chat_scripts` | 多角色剧本（A+B / A+B+C+D / A+B+C+D+E+F） |
| `warmups` | P0-P4 养号步骤 |
| `knowledge_bases` | 知识库（7 类） |
| `kb_sources` | KB 上传文档 |
| `kb_protected` | 风控禁答规则 |
| `faqs` | 知识库 FAQ |
| `assets` | 素材库（PHOTO/VIDEO/VOICE/DOCUMENT/TEXT_SNIPPET） |
| `leads` | 潜在客户 |
| `lead_candidates` | 候选人池 |
| `discovered_groups` | 群发现（爬群） |
| `groups` | 自建群 / 加入群 |
| `greeting_templates` | 问候语 |
| `platform_ai_configs` | 平台 fallback AI key |
| `platform_settings` | 平台级 settings (key-value) |
| `bot_replies` | Bot 自动回复历史 |

迁移：`apps/server/src/database/migrations/`，目前用 `synchronize: true`（生产风险，待替换为正规 migrations）。

---

## 12. 核心业务模块

### 12.1 Campaign 4 层架构

```
计划（Campaign）
  └─ 素材（AdTemplate）
       └─ 执行（Task）── 300/30 = 10 封/号 ── Gaussian 间隔
            └─ 归因（Attribution）
```

AI Variant 文案变体：
- 句式 / emoji / 标点 / 格式微调
- 图片像素级微偏移
- N 种变体相似度 < 70%

### 12.2 ChatScript 剧本引擎

```
A+B 2 人对话 / A+B+C+D 4 人 / A+B+C+D+E+F 6 人
  ├─ 群有真人活跃 → 跳过该群当天
  ├─ 冷场（XX 分钟没人发言）→ 触发执行
  └─ 一天一群一次（铁律）
```

剧本来源：
- DB migration `1730600000000-seed-builtin-6p-chat-scripts.ts`：10 个 6 人剧本
- SeedPack `script-packs/scripts_pack_ab_zh_v1.json`：30 个 2 人剧本
- SeedPack `script-packs/scripts_pack_4p_zh_v1.json`：50 个 4 人剧本
- 总计 90 个内置剧本，租户可自加

### 12.3 Warmup P0-P4（7 天养号）

| 阶段 | 行为 |
|------|------|
| P0 | 初始化（上线、加载 contacts） |
| P1 | 沉默观察（不发言、加 1-2 群） |
| P2 | 轻微活动（加好友确认、点赞） |
| P3 | 社交建立（发朋友圈/状态、回复消息） |
| P4 | 常规运营（执行 Campaign、ChatScript） |

所有动作走 Gaussian 随机间隔。

### 12.4 知识库 7 类

```
- 产品资料 (PRODUCT)
- 价格套餐 (PRICING)
- 售前 FAQ (PRESALES_FAQ)
- 售后 FAQ (POSTSALES_FAQ)
- 公司介绍 (COMPANY)
- 广告素材 (AD_MATERIAL)
- 风控禁答规则 (FORBIDDEN)
```

技术：pgvector + RAG。Starter FAQ 用 `{tenantName}` / `{botName}` 占位符。

### 12.5 素材库（Assets）

文件：`apps/server/src/assets/`

| Category | 用途 |
|----------|------|
| `PHOTO` | 图片 |
| `VIDEO` | 视频 |
| `VOICE` | 语音（OGG/Opus/MP3） |
| `DOCUMENT` | 文档 |
| `TEXT_SNIPPET` | 文本片段 |

| Source | 用途 |
|--------|------|
| `BUILTIN` | 内置（tenantId=null，所有租户共享）|
| `UPLOAD` | 租户自传 |
| `GENERATED` | AI 生成 |

存储双策略：
- 小文件（< 5MB）→ DB bytea 列
- 大文件（builtin）→ 磁盘 + `relativePath` 指针

`onModuleInit` 扫盘（vmfix20）：自动扫 `data/assets/_builtin/` 注册到 DB。

### 12.6 Lead Collection + Takeover

| 功能 | 修了什么 |
|------|---------|
| Lead | 结构化字段：tg_username + campaign 归因 + 产品 + 规模 + 预算 + 意向（cold/warm/hot）+ 是否需人工 |
| Takeover | 接管即时停 AI + 阻断广告任务 + 进人工池 + 全程 audit |

WAhubX 老问题（Takeover 不停 AI），TeleHubX 一开始就修对。

---

## 13. AI 集成

### 三档智能回复

| 档 | 行为 |
|----|------|
| `off` | 完全关闭 |
| `faq` | 只匹配关键词（FAQ 关键字匹配 + 知识库 RAG） |
| `smart` | 走 AI Smart Reply（LLM） |

### Provider 支持

| Provider | 实现 |
|----------|------|
| OpenAI | `openai` SDK 直连 |
| DeepSeek | OpenAI-compat baseURL |
| Gemini | OpenAI-compat baseURL |
| Claude | Anthropic SDK |
| Custom OpenAI-Compat | 自定义 baseURL |

### 双层 AI Key

```
租户客户聊天：tenant.aiKey（存 tenant_settings, AES-GCM 加密）
平台兜底（FAQ 生成等内部任务）：platform_ai_configs 表里的 default key
```

`effectiveAiConfig` 解析：
1. 任务带 tenant 上下文 → 用 tenant key
2. 没有 → fallback 到 platform default key
3. 都没有 → AI 操作 no-op，log warning

文件：`apps/server/src/ai-agent/ai-providers.ts` + `apps/server/src/platform-config/`

---

## 14. 安装包架构

### 主程序（vmfix25）

```
TeleHubX-Setup-1.0.0-vmfix25.exe (~145 MB)
├── Inno Setup 6 wrapper
├── 内置 SeedPack 内容（vmfix22 合并）
└── [Code] Pascal 脚本
    ├── PrepareToInstall: RemoveExistingService（sc stop + winsw uninstall + sc delete）
    ├── InitializeWizard: 创建 TgApiPage + DataDeletePage（vmfix25）
    ├── NextButtonClick: 校验 TG API 输入
    └── CurStepChanged(ssPostInstall):
        ├── ApplyAndVerifyServiceAccount（vmfix14：sc config + sc qc 验证）
        └── WriteEnvField TG_API → ProgramData\.env + .env.template（vmfix25）
```

### Wizard 页面顺序（vmfix25）

```
wpWelcome
  → wpLicense（已禁用，LicenseFile 空）
  → wpSelectDir
  → wpSelectComponents
  → wpSelectTasks（Tasks: desktopicon / autostart）
  → [TgApiPage]      ← 新增（vmfix25）
  → [DataDeletePage] ← 卸载策略选择
  → wpReady
  → wpInstalling
  → wpFinished（"启动 TeleHubX" 选项）
```

### [Run] 后置步骤

```
1. copy .env.template → ProgramData\TeleHubX\.env (if not exist)
2. WinSW install (注册 service)
3. sc.exe sdset TeleHubX D:(...) (vmfix20: 给 Authenticated Users 启停权限)
4. icacls grant LocalService Full Control on ProgramData\TeleHubX
5. icacls grant LocalService Read+Execute on Program Files\TeleHubX\runtime
6. icacls grant LocalService Read+Execute on Program Files\TeleHubX\apps
7. （可选 task）schtasks /Create AutoStart on logon
8. （可选）launch telehubx-start.vbs（postinstall checkbox）
```

### 桌面快捷方式（3 个）

| 名称 | 调用 | 行为 |
|------|------|------|
| **Start TeleHubX** | `wscript telehubx-start.vbs` | sc start + 拉起 mshta loading.hta（含 sc start 失败检测，vmfix20）|
| **Stop TeleHubX** | `wscript telehubx-stop.vbs` | sc stop |
| **TeleHubX Dashboard** | `wscript telehubx-open.vbs` | rundll32 url.dll FileProtocolHandler http://127.0.0.1:9601/（vmfix20，仅打开浏览器，不碰 service）|

### Start menu

包含上述 3 个 + `TeleHubX Debug`（显示日志的可见 PS）+ `Uninstall TeleHubX`

### WinSW 服务配置

文件：`installer/runtime/winsw/telehubx-service.xml`

```xml
<service>
  <id>TeleHubX</id>
  <executable>%BASE%\telehubx-supervisor.exe</executable>
  <startmode>Automatic</startmode>           <!-- vmfix20 -->
  <delayedAutoStart>true</delayedAutoStart>  <!-- vmfix20 -->
  <onfailure action="restart" delay="10 sec" />
  <onfailure action="restart" delay="30 sec" />
  <stoptimeout>30 sec</stoptimeout>
  <serviceaccount>
    <username>NT AUTHORITY\LocalService</username>
    <allowservicelogon>true</allowservicelogon>
  </serviceaccount>
  <env name="TELEHUBX_INSTALL_PATH" value="%BASE%\.." />
  <env name="TELEHUBX_DATA_DIR"     value="%ProgramData%\TeleHubX\data" />
  <env name="TELEHUBX_RUNTIME_MODE" value="prod" />
  <env name="NODE_ENV"              value="production" />
</service>
```

---

## 15. .env 加载链与候选优先级（**关键易错点**）

### 候选 .env 顺序

文件：`installer/tools/src/shared/env.ts` `loadSupervisorEnv()`

```
1. data/secrets/local-secrets.env             [auto-generated, JWT_SECRET 等]
2. %APPDATA%\TeleHubX\.env                    [LocalService userEnv — vmfix24 之前坑根]
3. <dataDir>\..\.env                           [%ProgramData%\TeleHubX\.env]
4. <installPath>\.env                          [Program Files\TeleHubX\.env]
5. <installPath>\.env.template                 [模板]
6. process.cwd()/.env                         [dev fallback]
```

**规则**：先到先得。已经在 `process.env` 里的 key（包括 WinSW XML 设置的）**不会**被后面的 .env 覆盖。

### LocalService userEnv 路径

```
C:\Windows\ServiceProfiles\LocalService\AppData\Roaming\TeleHubX\.env
```

**第一次启动时由 `bootstrapUserEnv()` 从 `.env.template` 拷贝过来**。后续启动直接读这个文件。

### vmfix24 修复

`PlatformSettingsService.saveTgApiAndRestart` 写**所有**候选路径：

```typescript
const candidates = [
  process.env.APPDATA + '\\TeleHubX\\.env',           // candidate #2
  process.env.TELEHUBX_DATA_DIR + '\\..\\.env',       // candidate #3
  process.env.TELEHUBX_DATA_DIR + '\\.env',           // candidate #4 (alt)
  process.env.TELEHUBX_INSTALL_PATH + '\\.env',
  process.cwd() + '\\.env',
];
for (const p of candidates) writeEnvField(p, key, value);
```

### vmfix25 修复

Inno Setup wizard 装包时直接写：
1. `%ProgramData%\TeleHubX\.env`
2. `%ProgramFiles%\TeleHubX\.env.template` ← 这个让 LocalService userEnv 在第一次启动时也带正确值

**两处都写**才能保证 supervisor 第一次读 candidate #2 时不撞空值。

---

## 16. SeedPack 内置素材

### 内容（vmfix22 起合并到主包）

| 类型 | 数量 | 大小 |
|------|------|------|
| 图片 | 97 | ~6 MB |
| 语音 | 200 | ~2.6 MB |
| 视频 | 25（精选） | ~36 MB |
| 聊天剧本 | 80 个 JSON pack | ~600 KB |
| **总计** | | **~45 MB** |

### 注册流程

1. 安装包 [Files] 释放到 `%ProgramData%\TeleHubX\data\assets\_builtin\` 和 `\script-packs\`
2. 服务第一次启动 → server `onModuleInit` 扫盘：
   - `AssetsService.onModuleInit()` 扫 `_builtin/` 自动 INSERT 到 `assets` 表（source=BUILTIN, tenantId=null）
   - `ChatScriptsService.onModuleInit()` 扫 `script-packs/*.json` 调用 `importPackBlob()`（已幂等）
3. 租户在 dashboard 看到内置素材库 + 内置剧本

### 独立 SeedPack 安装包（已废）

`installer/seedpack/` 仍存在，可以用 `installer/seedpack/build-seedpack.ps1` 单独 build 出 SeedPack-only `.exe`，但**不再是默认 ship 路径**。

---

## 17. 安全模型

### 网络隔离

```
所有服务: 127.0.0.1 only（loopback）
对外只: License Worker (Cloudflare HTTPS) + Telegram (HTTPS + MTProto via SOCKS5)
```

### 加密

| 资产 | 加密方式 | 密钥来源 |
|------|---------|---------|
| GramJS session string | AES-256-GCM | `SESSION_ENCRYPTION_KEY` (data/secrets/local-secrets.env) |
| Cloud license state | AES-256-GCM | 同上 |
| `agent-token.bin` | AES-256-GCM | 同上 |
| Proxy password | AES-256-GCM | 同上 |
| User password | scrypt(N=16384, r=8, p=1, len=64) + 16 byte salt | salt per-user |

### 敏感字段

`installer/scripts/secret-scan.mjs` 在每次 build 时扫 `installer/dist/` 禁止以下变量出现：

```
ADMIN_TOKEN / LICENSE_ADMIN_TOKEN / LICENSE_PEPPER
AGENT_TOKEN_SECRET / AGENT_TOKEN
USER_PASSWORD_PEPPER
JWT_SECRET / SESSION_ENCRYPTION_KEY
CLOUDFLARE_API_TOKEN
*_PRIVATE_KEY / *.pem
```

### 服务身份

```
Service: NT AUTHORITY\LocalService（非 admin）
进程 token: System Mandatory Level + NT AUTHORITY\SERVICE
PostgreSQL 通过 pg_ctl 启动时使用 CreateRestrictedToken 剥除 Administrators 成员（vmfix19）
```

### Roles 守卫

所有写操作走 `JwtAuthGuard` + `RolesGuard`，按 `@Roles()` 角色拦截。

### License 强校验

服务每 30 分钟向 License Worker verify，硬错误（revoked/suspended/expired）即时锁机器。

---

## 18. 构建与发布流程

### Build 主流程

```bash
cd C:\AI_WORKSPACE\Telegram Auto Bot
powershell installer\build.ps1
```

文件：`installer/build.ps1`

```
1. Vendor 完整性检查（node + postgres + redis 三套 portable 二进制）
2. 跑 installer/build-dist.cjs:
   ├─ 清 installer/dist/
   ├─ pnpm build server / agent / dashboard / installer-tools
   ├─ Assemble dist/apps/{server,agent,dashboard}（含 npm install --omit=dev flat node_modules）
   ├─ 拷 dist/tools 工具
   ├─ 拷 runtime init scripts + .vbs + WinSW XML + HTA
   ├─ 拷 vendor binaries
   ├─ 拷 .env.template
   ├─ 写 VERSION.txt
   ├─ 路径长度扫描（< 240 字符避免 ISCC 失败）
   └─ secret-scan installer/dist
3. SeedPack staging（vmfix22 起合并）:
   └─ installer/seedpack/build-seedpack.ps1 -StagingOnly
4. SEA 注入:
   ├─ supervisor.cjs → telehubx-supervisor.exe
   └─ stop.cjs → telehubx-stop.exe
5. ISCC 编译 installer/telehubx.iss → Output/TeleHubX-Setup-1.0.0-vmfixN.exe (~145 MB)
```

### 单独 build SeedPack（rare）

```powershell
powershell installer\seedpack\build-seedpack.ps1
# → Output/TeleHubX-SeedPack-1.0.0-vmfixN.exe (~45 MB)
```

### 发布

| 步骤 | 描述 |
|------|------|
| 1 | 上传 `.exe` 到 R2（Cloudflare）|
| 2 | 计算 SHA256 公布 |
| 3 | 用户下载会撞 Chrome SmartScreen / Windows SmartScreen（未签名）|
| 4 | 长期方案：买 EV 代码签名证书（~$300/年）|

### 版本号

每个 vmfix 自增。格式：

```
TeleHubX-Setup-1.0.0-vmfix{N}.exe
                |
                └─ 1.0.0 是 AppVersion，固定
                   vmfix{N} 是 BuildTag，每轮自增
```

VERSION.txt（在 `{app}\VERSION.txt`）记录：

```
version=vmfix{N}
commit={git hash}
buildTime={ISO}
artifact={file name}
serviceIdentity=NT AUTHORITY\LocalService
```

---

## 19. 已知问题与未来工作

### vmfix15-25 修复总结

| vmfix | 修了什么 |
|-------|---------|
| 15 | LicensePage `effectiveStatus='unknown'` 白屏 |
| 16 | dashboard 静态服务器没 reverse proxy → 405 |
| 17 | License 激活时不创建本地 user → 登录 401 |
| 18 | LicensePage 表单只在 unconfigured/locked 显示 + HTA 启动加载窗 |
| 19 | postgres 撞 admin-token check（pg_ctl 修复）+ HTA UTF-8 BOM |
| 20 | sc sdset 给 Authenticated Users 启停权限 + Auto-start + 第三个快捷方式 + builtin 素材扫盘 + SeedPack 独立包 |
| 21 | postmaster.pid race + SeedPack stop/start race + 孤儿 postgres 清理 |
| 22 | **合并 SeedPack 进主包** + /activate redirect 修复 |
| 23 | 自助 TG API 设置 modal + .env 自动写 + 自动重启 |
| 24 | .env 写所有候选路径 + 5 分钟超时 + ad-faq ADMIN access |
| 25 | **装包时 wizard 收 TG API**（根治）+ Settings 页编辑入口 |

### Phase 6 待办（未做）

| 项 | 说明 | 优先级 |
|----|------|------|
| schema-per-tenant | 每个租户独立 PG schema | 50+ 租户时必做 |
| 每租户独立 TG api_id | 平台维护 api_id 池，自动分配 | 5+ 租户时建议 |
| TypeORM migrations | 取代 `synchronize: true` | 生产前**必做**（数据风险） |
| JWT 路由守卫覆盖率 | 已建好但部分端点没 enforce | 中 |
| agent 端 AI 调用走 effectiveAiConfig | 当前 BotGateway 已切，cs MTProto 还没 | 中 |
| 代码签名 EV 证书 | 解决 SmartScreen 警告 | ship 必做 |

### 已知 cosmetic 问题（不阻塞）

| 项 | 说明 |
|----|------|
| stop.exe `exe_outside_install` 警告 | 跨 session OpenProcess 拿不到 exe path，supervisor SIGINT 兜底 |
| supervisor.log 里 `bootstrap lock 334s old` | 旧 supervisor 没干净 release lock，新 supervisor 会清 |
| init-pgdata warm pgdata 也要 2 分钟 | pg_ctl start -w 重试逻辑慢，可优化 |
| TYPEORM_SYNC=true in production | 危险但目前必须，待 migrations |

---

## 20. 快速排查手册

### Service 启动不了

```powershell
# 1. 看服务状态
sc.exe query TeleHubX

# 2. 看 supervisor 主日志
Get-Content "C:\ProgramData\TeleHubX\data\logs\supervisor.log" -Tail 80

# 3. 看 postgres 日志
Get-Content "C:\ProgramData\TeleHubX\data\logs\postgres.log" -Tail 30

# 4. 看 server 日志
Get-Content "C:\ProgramData\TeleHubX\data\logs\server.log" -Tail 50

# 5. 看端口
foreach ($p in 9601,9800,5436,6386) {
  $l = Get-NetTCPConnection -LocalPort $p -State Listen -EA 0
  if ($l) { "$p : LISTENING" } else { "$p : DOWN" }
}
```

### 强制重置（hard reset）

```powershell
sc.exe stop TeleHubX
Start-Sleep 5
taskkill /F /IM postgres.exe 2>$null
taskkill /F /IM redis-server.exe 2>$null
taskkill /F /IM telehubx-supervisor.exe 2>$null
Remove-Item "C:\ProgramData\TeleHubX\data\run\*.pid" -EA 0
Remove-Item "C:\ProgramData\TeleHubX\data\run\supervisor.lock" -EA 0
Remove-Item "C:\ProgramData\TeleHubX\data\pgdata\postmaster.pid" -EA 0
sc.exe start TeleHubX
```

### License 激活不行

```powershell
# 1. 查本地 license state
Test-Path "C:\ProgramData\TeleHubX\data\cloud-license.bin"

# 2. License Worker 健康
Invoke-WebRequest "https://telehubx-license.starbright-solutions.com/health" -UseBasicParsing

# 3. 本机激活流程
# 浏览器打开 http://127.0.0.1:9601/settings/license
# 填 License Key + Email + Password
# 看后端 server.log: "cloud-license activated: ..."

# 4. 如果 invalid_credentials → email 不对 / password 不对
# 5. 如果 machine_mismatch → 联系 SaaS admin 解绑该 license
```

### TG 绑号失败

```powershell
# 1. 查 TG_API 配置（vmfix25 wizard 应该已写）
Get-Content "C:\ProgramData\TeleHubX\.env" | Select-String "^TG_API_"

# 2. 走 dashboard 自助
# 浏览器：http://127.0.0.1:9601/settings/tg-api
# 填 api_id + api_hash → 保存 → 等服务重启

# 3. 查 BindService 日志
Get-Content "C:\ProgramData\TeleHubX\data\logs\server.log" -Tail 50 |
  Select-String "BindService|TG_API"
```

### Login 失败

```powershell
# 1. 确认本地有 user 行
# 后端 server.log 应有: "cloud-license: provisioned local user X (role=admin)"

# 2. 如果没有 → 重新走 /settings/license 重激活（vmfix17 hook 会创建）

# 3. 密码忘了？
# 在 License Server 改密码 → 重激活（vmfix17 mirror 会刷新本地密码）
```

### 服务跑得慢/吃 CPU

```powershell
# 看进程
Get-Process telehubx-supervisor,postgres,redis-server,node |
  Select-Object Id, ProcessName, CPU, WorkingSet
```

---

## 关键文件索引（开发者备查）

| 用途 | 路径 |
|------|------|
| **架构蓝图（v1.0）** | `telehubx_architecture_blueprint.md` |
| **完整计划（v2.0）** | `telehubx_full_plan.md` |
| **本文件** | `工程技术蓝图.md` |
| **Claude session 上下文** | `CLAUDE.md` |
| **上轮工作总结** | `HANDOFF.md` |
| supervisor 主逻辑 | `installer/tools/src/supervisor.ts` |
| .env 加载链 | `installer/tools/src/shared/env.ts` |
| stop 工具 | `installer/tools/src/stop.ts` |
| 安装脚本（含 wizard）| `installer/telehubx.iss` |
| WinSW 服务配置 | `installer/runtime/winsw/telehubx-service.xml` |
| HTA 加载窗 | `installer/runtime/launcher/telehubx-loading.hta` |
| 启动 .vbs | `installer/runtime/launcher/telehubx-start.vbs` |
| .env 模板 | `installer/.env.template` |
| Build 入口 | `installer/build.ps1` + `installer/build-dist.cjs` |
| SeedPack staging | `installer/seedpack/build-seedpack.ps1` |
| Cloud License Service | `apps/server/src/cloud-license/cloud-license.service.ts` |
| Auth Service | `apps/server/src/auth/auth.service.ts` |
| Tenant Resolver | `apps/server/src/auth/tenant-resolver.ts` |
| Bind Service | `apps/server/src/accounts/bind/bind.service.ts` |
| Platform Settings (TG API) | `apps/server/src/admin/platform-settings.service.ts` |
| Asset Scanner | `apps/server/src/assets/assets.service.ts` (onModuleInit) |
| ChatScript Importer | `apps/server/src/chat-scripts/chat-scripts.service.ts` (onModuleInit) |
| Dashboard 路由 | `apps/dashboard/src/App.tsx` |
| Dashboard API 客户端 | `apps/dashboard/src/services/api.ts` |
| LicensePage | `apps/dashboard/src/pages/license/LicensePage.tsx` |
| LoginPage | `apps/dashboard/src/pages/auth/LoginPage.tsx` |
| BindWizard | `apps/dashboard/src/pages/accounts/BindWizard.tsx` |
| TgApiSetupModal | `apps/dashboard/src/components/TgApiSetupModal.tsx` |
| TgApiPage（Settings）| `apps/dashboard/src/pages/settings/TgApiPage.tsx` |

---

## 联系与责任

- **项目所有者**: bryangeh79@gmail.com
- **GitHub**: https://github.com/bryangeh79/TeleHubX
- **License Server 部署**: Cloudflare Worker `telehubx-license.starbright-solutions.com`
- **构建机**: `C:\AI_WORKSPACE\Telegram Auto Bot`（Windows 11，Inno Setup 6 + pnpm + Node 20）

---

> 本文档随每轮 vmfix 更新。如有 vmfix26+，请同步更新章节 19 的 vmfix 列表 + 章节 20 的排查手册（如出现新坑）。

---

## 21. vmfix26 → vmfix29.1 增量交付（2026-05-12 ~ 2026-05-14）

> 本章是 vmfix25 之后、为新会话搬迁准备的完整增量记录。如要 30 分钟内接手 TeleHubX，**只读本章 + 第 22 章「新 Session 接手手册」即可**。

### 21.1 版本时间线

| 版本 | Commit | 日期 | 大小 | SHA256 | 关键交付 |
|---|---|---|---|---|---|
| vmfix26 | `26609b4` | 2026-05-12 | 145.24 MB | `d95cf91b22da9d1fba8cb7eb9bb32072440a67ddc8f2c8da223beb7b1c4f4d31` | 10 项 bugfix：wedge probe / stale client / 6p 剧本 seed / GramJS noise filter / 代理风险 Alert |
| vmfix27 | `9efd110` | 2026-05-13 | 145.25 MB | `73ad1c6b5de5ee9d9b4586fddcb5d3931dddb0518bbe2e550bb87d75634b22a2` | 关键词发现群强化 v1（13 项）：messages.searchGlobal / 邀请链接收割 / AI 关键词扩展 / quality 4 维 / 敏感群黑名单 / 增量发现 / 批量派发 |
| vmfix28 | `c979cb5` | 2026-05-13 | ~145 MB | — | 12 项完美工程：UX 时间戳 / discoverSource / 行内删除 / 内联上传 / AI 评分 / 热度 tag / 多账号 union / 群预览 / FloodWait 重派 / 发现+加群一体化 / 任务模板系统 |
| vmfix29 | — | 2026-05-14 | — | — | 9 项 UX 收口 + 滚雪球（A5 user.about / A6 forwarded source / A7 GetCommonChats）+ 派发账号记录 + 养号日志 + group_create 超时优化 |
| **vmfix29.1** | `a65891b` | 2026-05-14 | **145.29 MB** | `e9fbbdc5fc9d3e930728741df1400838c28f151aa6fe3d14577360286a3a5077` | **hotfix supervisor 启动超时（60→120s）+ 5 大关键词发现增强**（行业关键词包 / AI Suggest 按钮 / SEA 多语言 prompt / includeChannels 过滤 / 模板下拉入口）|
| **vmfix30** | — | 2026-05-14 | TBD | TBD | **4 bug 修 + Tier 2 patch 基建**：A1 chat_scripts.status default DRAFT→ACTIVE（修「指定具体剧本」下拉空）/ A2 service create 兜底 ACTIVE / A3 listPacks 加 tenant 隔离 / A4 supervisor heartbeat 监控 child alive + 自动重启 / **C 全部**：build.ps1 多产 `TeleHubX-Patch-{from}-to-{to}.zip`（5-10 MB）+ Apply-Patch.ps1（验 SHA256 + 备份 + 应用 + postPatchSql + 失败自动回滚）+ 桌面「Apply Patch」快捷方式。**新章节 24**「Patch 发布流程」已写入。|

### 21.2 vmfix29.1 下载地址（**租户分发用**）

```
https://download.starbright-solutions.com/TeleHubX-Setup-1.0.0-vmfix29_1.exe
```

**⚠️ 命名约定**：版本后缀使用**下划线**（`vmfix29_1`），**不是点**（`vmfix29.1`）。
原因：Inno Setup `OutputBaseFilename` 不允许 `.` 作分隔符（会被认成版本号），统一改下划线。
位于 Cloudflare R2：bucket `starbright-downloads`，custom domain `download.starbright-solutions.com`。

### 21.3 vmfix26 详细改动（10 项 bugfix）

| # | 文件 | 改动 |
|---|---|---|
| #4 | `apps/agent/src/main.ts` | 加 `isGramjsTeardownNoise()` + `unhandledRejection`/`uncaughtException` handler；命中 "Not connected"/"Disconnect"/"TIMEOUT" 静默吞，其它 warn 但不退出 |
| #6 | `apps/agent/src/tasks/task-runner.ts` + `apps/agent/src/main.ts` | `ServerCallbacks` 加 `getClient(accountId)`；reconnect 后取 fresh client 再递归 retry；同步更新 `clients` Map |
| #8 | `apps/server/src/main.ts` | 加同名 noise filter，server 端 1376 次 TIMEOUT 不再 process.exit |
| #11 | `apps/server/src/cloud-license/cloud-license.service.ts` | 网络错连续 < 3 次降 `info`，>= 3 才 `warn`（消 grace 期噪音）|
| #13 | `apps/agent/src/main.ts` heartbeat | 加 `getMe(5s timeout)` TG probe；wedge 立即 disconnect + connect 新 client |
| #14 | `apps/server/src/accounts/accounts.controller.ts` + `accounts.module.ts` | `/accounts/:id/warmup/start` 自动创建 `preset_warmup_7d` 任务；返回 `{plan, presetTask, presetWarning}` |
| #15 | `apps/server/src/chat-scripts/chat-scripts.service.ts` + 新 `data/script-packs/scripts_pack_6p_zh_v1.json` | 6 人剧本 10 条从 migration 抽到 SeedPack JSON；修 `detectType()` 识别 ABCDEF |
| #16 | `apps/dashboard/src/pages/accounts/AccountsPage.tsx` | 红色「代理风险警告」Alert（>=2 个号无代理/共用代理时触发）|
| #18 | `apps/dashboard/src/pages/cs/CsPage.tsx` | `!aiConfigured` 时 info Alert 引导去「设置 → AI 配置」 |
| #23 | `apps/dashboard/index.html` + `apps/dashboard/public/favicon.png` | favicon `/vite.svg` → `/favicon.png` |

### 21.4 vmfix27 详细改动（13 项 — 关键词发现群强化 v1）

| # | 模块 | 文件 | 用户感知 |
|---|---|---|---|
| D3 | 唯一索引验证 | (no code) | dedup 正确 |
| B1 | quality 4 维评分 | `apps/server/src/discovered-groups/discovered-groups.service.ts` | 修两极化（A 17/C 0/D 19）|
| B3 | 敏感群黑名单 | `apps/agent/src/tasks/sensitive-filter.ts` (新) + `executors.ts` | 自动剔除赌博/色情/跑分/政治极端群 |
| A1 | messages.searchGlobal | `apps/agent/src/tasks/executors.ts` `discoverGroupsByKeyword` | 召回 5-10× |
| A2 | 邀请链接收割 | `executors.ts` `discoverGroupsByInvites` (新) + `task.entity.ts` 新 TaskType + `SchedulerPage.tsx` | **解锁私密群** |
| A3 | AI 关键词扩展 | `apps/server/src/ai-agent/ai-agent.service.ts` `expandKeywords` + `/ai/expand-keywords` 端点 + agent server-callback | 单词输入 → 6 语义变体并发搜 |
| B2 backend | AI 评分 | `ai-agent.service.ts` `scoreGroupMatch` + `/ai/score-group` 端点 | 前端集成留 vmfix28 |
| C1 | 任务对话框升级 | `SchedulerPage.tsx` | AI 扩展 / 内容搜 / 敏感过滤 / 增量小时数 checkbox |
| C3 | 失败 fallback 文案 | `executors.ts` | 5 条 actionable 提示（含改写建议）|
| C4 | 增量发现 | `discovered-groups.service.ts` `findRecentByKeyword` + `/recent` 端点 + agent callback | 跳过最近 N 小时已发现 |
| C6 | 批量派发 A 档群 | `discovered-groups.controller.ts` `batch-queue-scrape` + `DiscoveredGroupsPage.tsx` | row selection + 一键加+爬 |
| D1 | 24h cache | 与 C4 同实现 | 同关键词 24h 内不重打 TG |

### 21.5 vmfix28 详细改动（12 项完美工程）

| # | 内容 | 文件 |
|---|---|---|
| #1 | 「发现时间」列（dayjs fromNow + 24h 内 NEW Badge）| `DiscoveredGroupsPage.tsx` |
| #2 | discoverSource 列 + 顶部 Alert + stats 写 errorMsg | `DiscoveredGroupsPage.tsx` + `discovered-group.entity.ts` (DiscoverSource enum) + `discovered-groups.service.ts` + `executors.ts` + `task-runner.ts` |
| #3 | 行内 delete 按钮 + Popconfirm | `DiscoveredGroupsPage.tsx` |
| #4 | ChatScriptEditor 内联上传 + 自动 poolName | `ChatScriptEditor.tsx` + `api.ts` (upload poolName 参数) + `assets.controller.ts/service.ts` |
| B2 | AI 评分前端集成 + 列 + 任务对话框 checkbox | `discovered-group.entity.ts` (aiScore/aiReason 列) + `executors.ts` + `DiscoveredGroupsPage.tsx` |
| B4 | recentMessageRate 列 + HOT tag + quality +5 加成 | `discovered-group.entity.ts` + `executors.ts` + `service.ts` |
| A4 | 多账号 union 搜（payload `multiAccountUnion`，max 2 个其它 client）| `executors.ts` Pass 3 |
| C5 | 群预览 Modal（静态 — DB 已有数据，不发 TG）| `DiscoveredGroupsPage.tsx` |
| C7 | Dashboard「关键词发现历史」widget | `DashboardPage.tsx` |
| D2 | FloodWait 跨账号 task 重派 | `tasks.service.ts` `reassignToAnotherAccount` + `tasks.controller.ts` 端点 + agent `task-runner.ts` + `server-callback.ts` |
| C2 | 「发现+加群」一体化（autoJoinAfterDiscover/Threshold/Max payload + `/auto-join-a-tier`）| `discovered-groups.controller.ts` + `executors.ts` + `server-callback.ts` |
| D4 | 任务模板系统（新 `TaskTemplate` entity + Service + Controller + onModuleInit seed 3 builtin）| `task-template.entity.ts` (新) + `task-templates.service.ts` (新) + `task-templates.controller.ts` (新) + `tasks.module.ts` |

### 21.6 vmfix29 详细改动（9 项 UX 收口 + 3 项滚雪球）

| # | 内容 | 文件 |
|---|---|---|
| NEW-1 | task 派发账号记录（dispatchedToAccountId/Label/At）| `discovered-group.entity.ts` + `discovered-groups.service.ts` |
| NEW-2 | 发现群清单 export | `DiscoveredGroupsPage.tsx` |
| NEW-3 | 养号日志 | `WarmupPage.tsx` + 后端 warmup-log entity |
| NEW-4 | payload 渲染美化 | `SchedulerPage.tsx` task detail modal |
| NEW-5 | 任务表单校验加强 | `SchedulerPage.tsx` |
| NEW-6 | group_create 超时优化（30s → 120s）| `apps/agent/src/tasks/executors.ts` `createGroup` |
| B2-frontend | 任务调度页「从模板创建」下拉入口 | `SchedulerPage.tsx` |
| A5 | user.about 挖 channel 链接 | `executors.ts` (snowball Pass 1) |
| A6 | 转发来源回溯 forwarded source | `executors.ts` (snowball Pass 2) |
| A7 | GetCommonChats 跨群成员展开 | `executors.ts` (snowball Pass 3) |

### 21.7 vmfix29.1 详细改动（hotfix + 5 关键词发现增强）

**Hotfix**：

| 项 | 文件 | 改动 |
|---|---|---|
| supervisor 启动超时 | `installer/tools/src/supervisor.ts` | server `healthTimeoutMs: 60_000 → 120_000`（vmfix27+28+29 累积初始化 ~80s 超 60s 触发"启动超时(5min)" splash 误报）|

**5 关键词发现增强**：

| # | 内容 | 文件 |
|---|---|---|
| **1. 行业关键词包** | 5 行业 × ~100 词（gambling / forex / crypto / beauty_wellness / education），混合中/英/马来/越南/印尼 + 城市细分（KL/JB/Penang/SG/BKK/HCM）| `apps/server/src/tasks/industry-keyword-packs.ts` (新) |
| **2. 8 个内置任务模板** | TaskTemplatesService.onModuleInit 在 vmfix28 的 3 个基础上 seed 5 行业包模板 | `apps/server/src/tasks/task-templates.service.ts` |
| **3. 行业包 API 端点** | `GET /task-templates/industry-packs` + `/industry-packs/:industry` | `apps/server/src/tasks/task-templates.controller.ts` |
| **4. SEA 多语言 AI prompt** | `expandKeywords` 系统提示词重写：强调 Bahasa Melayu / Tiếng Việt / Bahasa Indonesia / Thai + 城市细分 | `apps/server/src/ai-agent/ai-agent.service.ts` |
| **5. includeChannels 频道过滤** | `discoverGroupsByKeyword` payload 加 `includeChannels` (default false)，processChat 默认跳 channel kind | `apps/agent/src/tasks/executors.ts` |
| **前端入口** | `KeywordSuggestButton`（调 aiApi.expandKeywords 显示 chips Modal）+ `IndustryPackButton`（列 5 行业包，点击应用）+ `discoverIncludeChannels` checkbox | `apps/dashboard/src/pages/scheduler/SchedulerPage.tsx` |
| **API client** | `taskTemplatesApi.listIndustryPacks()` / `getIndustryPack(industry)` | `apps/dashboard/src/services/api.ts` |

### 21.8 schema 增量（vmfix26→29.1 加的列）

> 所有列通过 TypeORM `synchronize: true` 自动加，无需 migration。旧行 default 兜底向后兼容。

`discovered_groups` 表新增：

```sql
discoverSource           ENUM('contacts','global','invite_harvest','snowball') DEFAULT 'contacts'
aiScore                  INT NULL
aiReason                 VARCHAR(256) NULL
recentMessageRate        INT DEFAULT 0
dispatchedToAccountId    UUID NULL
dispatchedToAccountLabel VARCHAR NULL
dispatchedAt             TIMESTAMP NULL
```

新表：

```sql
task_templates (id, tenantId NULL, name, description, type, payload JSONB,
                isBuiltin BOOL, usageCount INT, createdAt, updatedAt)
```

新 enum 值：

```ts
TaskType.DISCOVER_GROUPS_BY_INVITES = 'discover_groups_by_invites'   // vmfix27
TaskType.DISCOVER_AND_JOIN          = 'discover_and_join'             // vmfix28
DiscoverSource.SNOWBALL             = 'snowball'                      // vmfix29
```

---

## 22. 新 Session 接手手册（搬迁聊天室必读）

### 22.1 项目身份卡

| 维度 | 值 |
|---|---|
| 项目 | **TeleHubX** — Telegram 广告投放 + 多账号养号 + AI 智能客服 + 人工接管 + 线索收集 SaaS |
| 路径 | `C:\AI_WORKSPACE\Telegram Auto Bot` |
| GitHub | https://github.com/bryangeh79/TeleHubX |
| Owner | bryangeh79@gmail.com |
| 平台 | Windows 11 / bash + PowerShell |
| 商业逻辑 | **广告号引流 → 客服号成交** |
| 参考项目 | WAhubX（`C:\AI_WORKSPACE\Whatsapp Auto Bot`，**只读不写**）|

### 22.2 技术栈速查

| 层 | 选型 |
|---|---|
| 后端 | NestJS + TypeORM + PostgreSQL 16 + Redis + BullMQ |
| TG 客户端 | **GramJS (TypeScript)** + StringSession（不用 TDLib/Telethon/Pyrogram）|
| 客服入口 | **Bot API**（单一 Bot，低风险）|
| 广告/养号 | **MTProto Client (GramJS)**（多账号）|
| 前端 | React 18 + Vite + Ant Design 5 + React Query + Zustand + Socket.IO |
| 部署 | Windows Service (WinSW) + Node SEA supervisor + Inno Setup 安装包 |
| 代理 | SOCKS5 住宅/移动代理（一号一固定 IP） |
| 设备指纹 | Samsung SM-S928B + Android 14 + Telegram 10.14.2 + 马来西亚住宅 IP |
| License | Ed25519 签名 + Cloudflare Workers 验证 |
| 多租户 | schema-per-tenant（设计中，当前单 schema 共享）|

### 22.3 三种 TG 账号角色（核心创新）

| 角色 | 行为 | 风险 |
|---|---|---|
| **客服号 (cs)** | AI 自动回复 + FAQ + 接管，**被动**等待客户 | 低 |
| **广告号 (ad)** | 关闭 AI；群组 FAQ 简短回复；私聊统一引流到 Bot；执行 Campaign / 剧本 / Warmup | 中 |
| **混合号 (hybrid)** | 高风险，需 Super Admin 显式覆盖才启用 | 高 |

### 22.4 整体架构（一图速记）

```
+------------------- Windows Service (TeleHubX) -------------------+
|  WinSW (telehubx-service.exe)  [auto-start, LocalService]        |
|       |                                                            |
|  telehubx-supervisor.exe (Node SEA)                               |
|       +-> postgres.exe         5436   (pg_ctl, restricted token)  |
|       +-> redis-server.exe     6386                                |
|       +-> node main.js (server) 9800  NestJS                      |
|       +-> node main.js (agent)        GramJS multi-account        |
|       +-> node serve.cjs (dash) 9601  reverse proxy /api -> 9800  |
+--------------------------------------------------------------------+
                                |
                                v Browser at 127.0.0.1:9601
                       +-----------------+
                       | React+AntDesign |
                       +-----------------+
                                |
                                v HTTPS
              +-------------------------------------+
              |  License Worker (Cloudflare)        |
              |  telehubx-license                   |
              |    .starbright-solutions.com        |
              |  - /licenses/issue   (admin)        |
              |  - /licenses/activate               |
              |  - /licenses/verify  (periodic)     |
              |  - /agents/heartbeat (periodic)     |
              +-------------------------------------+
```

### 22.5 端口速查

| 服务 | 端口 |
|---|---|
| Backend (NestJS) | **9800** |
| Dashboard (serve.cjs reverse proxy) | **9601** |
| PostgreSQL | **5436** |
| Redis | **6386** |

### 22.6 License 部署（完整流程）

#### 架构

- **License 中心**：Cloudflare Worker，域名 `telehubx-license.starbright-solutions.com`
- **签名**：Ed25519 私钥（在 Worker 端 KV 持久化），public key 编译进 client
- **激活产物**：`<dataDir>/cloud-license.bin`（用 device fingerprint + passphrase 加密）
- **fp**：device fingerprint，hash(machineGuid + macAddr + userSid)，存 `<dataDir>/.fp`

#### License 端点

| 端点 | 谁调 | 频率 | 作用 |
|---|---|---|---|
| `POST /licenses/issue` | 平台 admin（你）| 手动 | 签发新 license key（绑 tenantId/email/期限）|
| `POST /licenses/activate` | 租户客户端首次启动 | 一次 | 用 key + email + fp 换签名的 `cloud-license.bin` |
| `POST /licenses/verify` | 客户端 server | 每 30 分钟 | 校验未撤销 + 未过期；3 次失败降级 warn；超过 grace 期暂停业务 |
| `POST /agents/heartbeat` | 客户端 server | 每 5 分钟 | 上报租户存活状态、版本号 |

#### 客户端 License 路径

```
<dataDir>/cloud-license.bin           # 加密的 license payload
<dataDir>/.fp                          # device fingerprint hex
<dataDir>/secrets/local-secrets.env   # JWT_SECRET (auto-gen)
```

`<dataDir>` 解析顺序（**关键易错点**）：

1. `process.env.TELEHUBX_DATA_DIR`（WinSW XML 中显式设；dev 在 `ecosystem.config.cjs` 必须设）
2. fallback `os.homedir()/TeleHubX/data`（**dev 机踩坑点**：若 NODE_ENV=production 但漏设 TELEHUBX_DATA_DIR，会走到 ghost dir）

→ **vmfix26 之后修复**：`ecosystem.config.cjs` 强制为 server + agent 设 `TELEHUBX_DATA_DIR=C:\AI_WORKSPACE\Telegram Auto Bot\data`

#### 客户端激活流程（租户视角）

1. 装 vmfix29.1 安装包 → wizard 步骤填 TG api_id/api_hash → 自动跑服务
2. 浏览器自动开 http://127.0.0.1:9601 → `/activate` 或 `/license` 页
3. 输入 license key + email → 后端调 Worker `/licenses/activate` → 拿签名 bin 写盘
4. 自动跳 `/login` → 用 email + 临时密码（由 `provisionLocalUser` 自动生成或激活时 set）登录
5. 进入 dashboard `/accounts` → 绑 TG 账号 → 开工

#### License 失效排查 checklist

| 症状 | 检查 |
|---|---|
| `/cloud-license/status` 返 `unconfigured` | (a) `<dataDir>/cloud-license.bin` 存在吗？(b) server 进程的 cwd / TELEHUBX_DATA_DIR 指对了吗？(c) `.fp` 匹配吗？ |
| 激活时 405 | dashboard reverse proxy `/api/*` 没生效（vmfix16 修过，回归注意 `serve.cjs`）|
| 激活时 401 | Worker 端 license key 已撤销，或 email 不匹配 |
| 激活 ok 但 `/login` 401 | `provisionLocalUser` 没跑（vmfix17 修过，注意 NestJS guard 顺序）|

### 22.7 凭据 / 机密（**不入 repo**）

> 测试机租户凭据 / license key / TG api_hash / SUPER_ADMIN 密码等**机密内容**，统一保存在仓库根目录 `.credentials/SESSION_HANDOFF_2026-05-14.md`（已 `.gitignore`）。
> 新 Session 接手时如要登录测试机，找该文件即可；本 .md 与 PDF 不重复落地，避免 git push 泄漏。
>
> 同时生成的 PDF 副本 `TeleHubX-技术蓝图-vmfix29.1.pdf`（仅本机持有，不入 repo）会**包含**这些凭据，方便 Bryan 手机/平板查阅。

### 22.8 六大核心业务模块（一句话总结）

| 模块 | 一句话 | 详细位置 |
|---|---|---|
| **Campaign 4 层** | 计划→素材→执行→归因；300 客户/30 号 = 10 封/号；Gaussian 间隔；AI Variant 文案变体 | 第 12 章 + WAhubX `campaigns/` 复用 |
| **ChatScript 多角色剧本** | A+B+C+D（+E+F）多账号在群内模拟对话；冷场才执行；一天一群一次 | `chat-scripts.service.ts` |
| **Warmup P0-P4** | 7 天周期养号；初始化→沉默→轻活动→社交→常规 | `apps/agent/src/warmup/` |
| **AI 智能回复** | 三档 off / faq / smart；Provider: OpenAI/DeepSeek/Gemini/Claude/Custom；tenant 级配置 | `intelligent-reply/` + `tenant_settings` |
| **Lead Collection** | 结构化字段（username/campaign/产品/规模/预算/意向/接管）+ CSV 导出 | `apps/server/src/leads/` |
| **Takeover 人工接管** | 接管→AI 立停+广告任务跳过该客户+人工池+audit（修 WAhubX 缺陷）| `apps/server/src/takeover/` |

知识库 7 类：产品资料 / 价格套餐 / 售前 FAQ / 售后 FAQ / 公司介绍 / 广告素材 / 风控禁答规则。

### 22.9 关键词发现群（vmfix27→29.1 重头戏）

**5 种发现机制**：

1. **contacts.Search**（按群名子串）— 上限 30 条，老路径
2. **messages.searchGlobal**（按消息内容全网搜公开群）— vmfix27 加，召回 5-10×
3. **AI 关键词扩展**（输 1 词 → AI 展开 6 语义变体并发搜）— vmfix27 加
4. **邀请链接收割**（进种子群扫消息 `t.me/+xxx`，含**私密群**）— vmfix27 加
5. **滚雪球**（A5 user.about / A6 forwarded source / A7 GetCommonChats）— vmfix29 加

**质量评分**：B1 重做的 4 维（成员数+活跃度+相关度+kind 各 25）+ B4 热度 +5 加成 + B2 AI 评分（0-100）。

**5 个内置行业关键词包**（vmfix29.1）：gambling / forex / crypto / beauty_wellness / education，每包 ~100 词，覆盖中英马越泰印 + KL/JB/Penang/SG/BKK/HCM 城市细分。

**任务模板系统**（vmfix28+29.1）：8 个 builtin（3 基础 + 5 行业包）+ 租户自建。

### 22.10 安装包构建流程（出新 vmfix）

```powershell
cd "C:\AI_WORKSPACE\Telegram Auto Bot"

# 1. 改代码 + dev 验证
pnpm --filter @telehubx/server build
pnpm --filter @telehubx/dashboard build
pnpm --filter @telehubx/agent build
pm2 restart all

# 2. bump 版本号（两处必须同步）
#    installer/build-dist.cjs   - VERSION.txt 内容 + artifact 名（注意下划线 vmfixXX_Y）
#    installer/telehubx.iss     - OutputBaseFilename（同样下划线）

# 3. build 安装包（~6 分钟）
powershell -NoProfile -ExecutionPolicy Bypass -File installer\build.ps1

# 4. SHA256
powershell -Command "(Get-FileHash 'installer\Output\TeleHubX-Setup-1.0.0-vmfixXX_Y.exe' -Algorithm SHA256).Hash.ToLower()"

# 5. commit + push
git add -A && git commit -m "release(vmfixXX.Y): ..." && git push origin main

# 6. 上传 R2（Bryan 手动 Cloudflare dashboard 上传到 starbright-downloads bucket）

# 7. 下载 URL
# https://download.starbright-solutions.com/TeleHubX-Setup-1.0.0-vmfixXX_Y.exe
```

### 22.11 关键技术地雷（务必记住）

> 完整列表见第 20 章「快速排查手册」+ HANDOFF.md 第 5 节。这里只列搬迁最易踩的：

1. **.env 加载先到先得**：`<dataDir>/secrets/local-secrets.env` → `%APPDATA%/TeleHubX/.env`（LocalService userEnv）→ `<dataDir>/../.env` → `<install>/.env` → `<install>/.env.template`。第一个候选已读到 key 后**续候选不覆盖**。
2. **postgres 不能在 admin token 进程下启**：必须 `pg_ctl start`（自带 CreateRestrictedToken）。
3. **PowerShell `$Args` 是自动变量**：别用做 param 名。
4. **Inno Setup `{` 是常量解析符**：[Run] Parameters 含 `{` 要转义 `{{`。
5. **OneDrive 桌面 ≠ `$USERPROFILE\Desktop`**：dev 机用 `[Environment]::GetFolderPath('Desktop')`。
6. **TELEHUBX_DATA_DIR 必设**：vmfix26 dev 机 license 失效的根因，详见 21.3 + HANDOFF.md 0.5。
7. **版本号 vmfix29.1 用下划线**：R2 文件名是 `vmfix29_1`，不是 `vmfix29.1`。Inno OutputBaseFilename 不允许 `.`。
8. **GramJS teardown noise**：disconnect/reconnect 时 GramJS 内部 reject "Not connected" / "TIMEOUT" 是正常的，必须 `unhandledRejection` 兜底（vmfix26 #4 + #8 已修）。

### 22.12 待办（vmfix30+ 候选，**纯工程无月费**）

| 项 | 工时 | 触发条件 |
|---|---|---|
| **C5 live peek**（实时拉群最近消息预览，新 task type `group_peek`）| 3h | 用户「预览」按钮使用频次高时 |
| **E2 行业关键词包前端「应用行业包」UX 强化**（已有 5 包，再加批量替换 / 多选合并）| 1-2 天 | 看租户行业集中度，80% 同行业立刻做 |
| **TypeORM migrations**（取代 synchronize:true）| 3-5 天 | 真正发租户前必做 |
| **JWT 路由守卫落地**（个别 @Public 未对齐）| 1 天 | 同上 |
| **代码签名 EV 证书**（~$300/年）| — | 解决 SmartScreen 警告 |
| **Phase 6 SaaS**：schema-per-tenant + 每租户独立 api_id 池 | 2-3 周 | 5+ 租户时 |

**已排除**（用户 2026-05-14 决策——"要额外付费的就不要了"）：

- ~~A8 tgstat.com 商业接入~~（月费 $49-149）
- ~~E1 自建 mini-indexer~~（服务器+IP 月费 $200-500）

### 22.13 新 Session 开机检查

```powershell
cd "C:\AI_WORKSPACE\Telegram Auto Bot"
git log --oneline -10
# 期望 HEAD = a65891b (vmfix29.1) 或更新

git status
# 期望 clean

ls installer\Output\TeleHubX-Setup-1.0.0-*.exe
# 期望看到 vmfix26 / vmfix27 / vmfix28 / vmfix29 / vmfix29_1

pm2 status
# 期望 telehubx-server / telehubx-agent / telehubx-dashboard 全部 online
```

### 22.14 给新 Claude session 的开场建议

1. **先读本章（21+22）+ HANDOFF.md 0.5/G/H/I 节** — 最近一轮全景
2. **再扫第 1-20 章** — 完整技术参考
3. **再读 `CLAUDE.md` 第七节** — 高层进度
4. WAhubX 路径 `C:\AI_WORKSPACE\Whatsapp Auto Bot`，**只读不写**
5. **不要主动重做已修过的事**（vmfix15→29.1 全 ship 历史在 21.1 表中）
6. **Bryan 偏好**：
   - 不要主动创建 *.md 文档（除非他要求）
   - 不要写 emoji 进文件（除非他要求）
   - commit 信息中英文都可，按 WAhubX 风格
   - 编辑 `工程技术蓝图.md` / `HANDOFF.md` 后要 push GitHub
   - 复杂决策（架构 / 安装包结构）要先 plan 再批
   - "全过了 = ship-ready" 是验收标准，不能用"代码看着对"糊弄

---

## 24. Tier 2 Patch 发布流程（vmfix30+）

> 老租户每发一个小 bugfix 不再下载 145 MB 完整安装包，改下 5-10 MB patch zip。
> Full installer 仍每版构建，给**新租户**首次安装用。

### 24.1 发版者操作（你 = Bryan）

```powershell
cd "C:\AI_WORKSPACE\Telegram Auto Bot"

# 1. 改代码
# 2. 编辑/新建 installer/patches/<targetVersion>.json：
#    - fromVersions: 可接受的旧版本列表（如 ["vmfix29.1", "vmfix29"]）
#    - toVersion:    本次版本号
#    - payload:      要替换的目录/文件列表（dist 路径）
#    - postPatchSql: 应用完代码后跑的 SQL（数据 hotfix 用）
#    - releaseNotes: 给租户看的说明
# 3. bump 版本号：
#    - installer/build-dist.cjs 里 version=vmfixNN
#    - installer/telehubx.iss 里 OutputBaseFilename
# 4. 一键构建（生成 full installer + patch zip）：
.\installer\build.ps1 -PatchVersion vmfix30

# 输出：
#   installer\Output\TeleHubX-Setup-1.0.0-vmfix30.exe                       (~145 MB, 给新租户)
#   installer\Output\TeleHubX-Patch-vmfix29.1-to-vmfix30.zip                (~5-10 MB, 给老租户)
#   installer\Output\TeleHubX-Patch-vmfix29.1-to-vmfix30.zip.sha256         (sidecar 完整性校验)

# 5. 上传 R2（手动 dashboard 上传，或 wrangler）
# 6. 通知租户（邮件 / 微信）：
#    "请下载 patch 应用：
#     https://download.starbright-solutions.com/TeleHubX-Patch-vmfix29.1-to-vmfix30.zip
#     SHA256：<复制 sidecar 内容>
#     双击桌面「TeleHubX Apply Patch」快捷方式 → 选这个 zip → 确认即可。
#     约 2 分钟下载 + 1 分钟应用，期间服务短暂重启。"
```

### 24.2 租户操作

1. 收到通知，下载 `.zip` + `.zip.sha256` 两个文件
2. 双击桌面快捷方式 **「TeleHubX Apply Patch」**
3. UAC 弹窗 → 允许
4. 文件选择器 → 选刚下载的 zip
5. 确认 release notes → 点 Yes
6. ~90 秒后 PowerShell 窗口显示 `DONE` → 浏览器刷新 dashboard 验证
7. **失败时自动回滚** —— 不需要手动操作

### 24.3 Apply-Patch.ps1 内部流程

```
1. 验 SHA256（zip 与 sidecar 比对）
2. 解 patch-manifest.json
3. 版本门控：检查 VERSION.txt ∈ fromVersions
4. 显示 release notes + 确认
5. 备份 payload 列出的目录/文件到 <dataDir>\patches-backup\<ts>\
6. Stop-Service TeleHubX
7. 解压 payload 覆盖 C:\Program Files\TeleHubX\
8. 写新 VERSION.txt
9. Start-Service + 探测 http://127.0.0.1:9800/health
10. 跑 postPatchSql（用 .env 里 DB_PASSWORD 通过 psql.exe）
11. 任何一步失败 → 自动回滚（恢复备份 → 重启服务 → 退出非零）
```

### 24.4 限制（哪些改动**不能**走 Tier 2 patch）

| 改动类型 | 能走 patch? | 原因 |
|---|---|---|
| `apps/*/dist/` 任何 .js / .json / .css | ✅ | payload 涵盖 |
| `apps/dashboard/dist/index.html` | ✅ | 同上 |
| `installer/tools/telehubx-supervisor.exe` | ✅ | payload 涵盖 |
| node.exe 版本升级 | ❌ | `runtime/node/` 不在 patch payload |
| postgres 版本升级 | ❌ | `runtime/postgres/` 同上 |
| 新增 `.env` 字段 | ⚠️ | postPatchSql 可写 `INSERT INTO settings`，但 .env 文件 patch 不动 |
| Inno Setup wizard 改动 | ❌ | 必须发 full installer |
| WinSW 服务配置改动（XML）| ⚠️ | payload 可加 `tools/telehubx-service.xml`，但**改完要 `telehubx-service.exe stop; install`** Apply-Patch 现版不支持，需 full installer |
| 桌面快捷方式（.lnk）| ❌ | Inno [Icons] 段控，必须 full installer |

新租户必须用 **full installer**，因为他们没有 Apply-Patch.ps1。

### 24.5 patch zip 物理结构

```
TeleHubX-Patch-vmfix29.1-to-vmfix30.zip
├── patch-manifest.json
└── payload/
    ├── apps/
    │   ├── server/dist/...
    │   ├── agent/dist/...
    │   └── dashboard/dist/...
    └── tools/
        └── telehubx-supervisor.exe
```

`patch-manifest.json` 顶层字段：`schemaVersion`, `patchId`, `fromVersions[]`, `toVersion`, `buildAt`, `payload[]`, `postPatchSql[]`, `releaseNotes`。

### 24.6 旁路工具

| 用途 | 命令 |
|---|---|
| 试跑不应用 | `.\Apply-Patch.ps1 -PatchZip ... -DryRun` |
| 跳过备份（不推荐）| `.\Apply-Patch.ps1 ... -NoBackup` |
| 应用但不重启服务（测试）| `.\Apply-Patch.ps1 ... -NoServiceRestart` |
| 静默（CI 用）| `.\Apply-Patch.ps1 ... -Yes` |

### 24.7 路径速查

| 文件 | 位置 |
|---|---|
| Apply-Patch.ps1（运行时）| `C:\Program Files\TeleHubX\tools\Apply-Patch.ps1` |
| Apply-Patch VBS 包装器 | `C:\Program Files\TeleHubX\tools\telehubx-apply-patch.vbs` |
| Patch 元数据（构建期）| `installer\patches\<version>.json` |
| Patch 构建器 | `installer\scripts\build-patch.ps1` |
| Patch 备份目录（运行时）| `C:\ProgramData\TeleHubX\data\patches-backup\<ts>\` |

---

## 25. 联系与责任

- **项目所有者**: bryangeh79@gmail.com
- **GitHub**: https://github.com/bryangeh79/TeleHubX
- **License Server 部署**: Cloudflare Worker `telehubx-license.starbright-solutions.com`
- **下载分发**: Cloudflare R2 bucket `starbright-downloads`（custom domain `download.starbright-solutions.com`）
- **构建机**: `C:\AI_WORKSPACE\Telegram Auto Bot`（Windows 11，Inno Setup 6 + pnpm + Node 20）

---

> **本文档每轮 vmfix 后更新**。最后更新：**2026-05-14（vmfix30）**。
> 下一轮如出 vmfix31+，同步更新章节 19（vmfix 列表）、章节 21.1（时间线表）、章节 22.12（待办清单），并在 `installer/patches/` 新建 `vmfix31.json`。
